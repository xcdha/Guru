# MyYoda v0.11.1 更新

## Fixes（问题修复）

- **修复用户消息气泡换行异常**：短消息不再被折成多行（如「上游有新更新了」折成 4+2 字），长消息在容器边缘正常折行且开头不再被裁切。气泡宽度改用 fit-content 自适应：空间充足时贴合内容单行显示，超宽时收缩到容器并正常断行
- **修复长消息开头被裁切**：去掉导致气泡被强制撑成单行宽的 min-width 约束，配合 overflow-wrap:anywhere 保证任意内容（含长链接、无空格串）都能正常断行
